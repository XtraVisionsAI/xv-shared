{
  "name": "@xv-shared/root",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.18.2",
  "engines": {
    "node": ">=18",
    "pnpm": ">=10"
  },
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "catalog:"
  }
}
