{
  "name": "@xv-shared/root",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "catalog:"
  }
}
